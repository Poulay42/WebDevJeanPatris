{% extends 'base.html.twig' %}

{% block title %}Nous contacter{% endblock %}

{% block body %}
    <div class="col-md-8 offset-2">
        <h1>Nous contacter</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn-outline-primary">Envoyer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
